/* eslint-disable jsx-a11y/alt-text */
import {Image,Page,StyleSheet,Text,View} from "@react-pdf/renderer";import type {ModelGuide} from "../types/ModelGuide";import {translate} from "@/features/i18n/lib/i18n";import {guidePdfStyles,pdfColors} from "./guidePdfStyles";import {GuidePageFooter} from "./GuidePageFooter";import {getGuideSettings} from "../lib/guideSettings";
const styles=StyleSheet.create({card:{borderColor:pdfColors.border,borderRadius:10,borderWidth:1,borderStyle:"solid",padding:14,marginBottom:14},step:{fontSize:9,color:pdfColors.accent,fontWeight:700},title:{fontSize:15,fontWeight:700,marginTop:4},description:{fontSize:9,color:pdfColors.muted,marginTop:6},image:{height:250,objectFit:"contain",backgroundColor:pdfColors.surface,marginTop:12},parts:{flexDirection:"row",flexWrap:"wrap",gap:5,marginTop:10},part:{fontSize:8,backgroundColor:pdfColors.surface,padding:5}});
export function GuideAssemblyPages({guide}:{guide:ModelGuide}){const settings=getGuideSettings(guide),locale=guide.locale??"en",t=(key:Parameters<typeof translate>[1],values?:Parameters<typeof translate>[2])=>translate(locale,key,values);return <Page size="A4" style={guidePdfStyles.page} wrap><Text style={guidePdfStyles.eyebrow}>{t("guide.assembly.eyebrow")}</Text><Text style={guidePdfStyles.pageTitle}>{t("guide.assembly.title")}</Text><Text style={guidePdfStyles.sectionDescription}>{t("guide.assembly.description")}</Text>{guide.assemblySteps?.map(step=><View key={step.id} style={styles.card} wrap={false}><Text style={styles.step}>{t("guide.assembly.step",{number:String(step.order).padStart(2,"0")})}</Text><Text style={styles.title}>{step.title}</Text>{step.description?<Text style={styles.description}>{step.description}</Text>:null}{settings.includeAssemblyStepImages?(step.image?<Image src={step.image} style={styles.image}/>:<View style={[styles.image,{alignItems:"center",justifyContent:"center"}]}><Text>{t("guide.assembly.imageMissing")}</Text></View>):null}<View style={styles.parts}>{step.parts.map(part=><Text key={part.id} style={styles.part}>{String(part.number).padStart(2,"0")} — {part.name}</Text>)}</View></View>)}<GuidePageFooter pageNumber={1} locale={locale}/></Page>}
