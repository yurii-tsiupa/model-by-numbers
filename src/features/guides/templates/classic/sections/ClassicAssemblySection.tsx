/* eslint-disable @next/next/no-img-element */
import type { GuideAssemblyStep } from "../../../types/ModelGuide";
import type { TranslationKey } from "@/features/i18n/locales/en";
import { classicPreviewStyles as styles } from "../classic.styles";
import { ClassicSectionHeading } from "../ClassicSectionHeading";
export function ClassicAssemblySection({steps,showImages,t}:{steps:readonly GuideAssemblyStep[];showImages:boolean;t:(key:TranslationKey,values?:Readonly<Record<string,string|number>>)=>string}){return <section className={styles.section}><ClassicSectionHeading eyebrow={t("guide.assembly.eyebrow")} title={t("guide.assembly.title")} description={t("guide.assembly.description")}/><div className="mt-8 grid gap-5 lg:grid-cols-2">{steps.map(step=><article key={step.id} className={styles.card}>{showImages?(step.image?<img src={step.image} alt={step.title} className="aspect-[4/3] w-full bg-black/20 object-contain"/>:<div className="flex aspect-[4/3] items-center justify-center text-neutral-500">{t("guide.assembly.imageMissing")}</div>):null}<div className="p-5"><p className="text-xs text-orange-300">{t("guide.assembly.step",{number:String(step.order).padStart(2,"0")})}</p><h3 className="mt-2 text-lg font-semibold">{step.title}</h3>{step.description?<p className="mt-2 text-sm text-neutral-400">{step.description}</p>:null}<div className="mt-4 flex flex-wrap gap-2">{step.parts.map(part=><span key={part.id} className="rounded-full border border-white/10 px-2 py-1 text-xs">{String(part.number).padStart(2,"0")} — {part.name}</span>)}</div></div></article>)}</div></section>}
