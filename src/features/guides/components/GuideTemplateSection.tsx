"use client";
import { useState } from "react";
import { useTranslation } from "@/features/i18n/hooks/useTranslation";
import { TemplatePickerModal } from "./TemplatePickerModal";
import type { GuideLibraryTemplate, UserGuideTemplate } from "@/features/templates/types/GuideLibraryTemplate";

export function GuideTemplateSection({current,userTemplates,isSelecting,onSelect}:{current:GuideLibraryTemplate;userTemplates:readonly UserGuideTemplate[];isSelecting:boolean;onSelect:(id:string)=>Promise<void>}){const{t}=useTranslation();const[open,setOpen]=useState(false),[notice,setNotice]=useState(false);const name=current.source==="builtIn"?t(`templates.presets.${current.nameKey}`):current.name;async function select(id:string){await onSelect(id);setOpen(false);setNotice(true)}return <section className="mb-5 rounded-2xl border border-[var(--border)] bg-[var(--card)] p-4"><p className="text-xs font-medium uppercase tracking-wide text-[var(--text-secondary)]">{t("guide.template.current")}</p><div className="mt-2 flex flex-col gap-3 sm:flex-row sm:items-center"><div className="min-w-0 flex-1"><h2 className="truncate text-base font-semibold">{name}</h2><p className="mt-1 text-xs text-[var(--text-secondary)]">{t(`templates.categories.${current.category}`)} · {t(current.source==="builtIn"?"guide.template.builtIn":"guide.template.mine")}</p></div><button type="button" onClick={()=>{setNotice(false);setOpen(true)}} className="min-h-10 rounded-[10px] bg-[var(--accent)] px-4 text-sm font-medium text-[var(--accent-foreground)] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[var(--accent)]">{t("guide.template.change")}</button></div>{notice?<p role="status" className="mt-3 text-xs text-[var(--accent-2)]">{t("guide.template.applied")}</p>:null}{open?<TemplatePickerModal currentId={current.id} userTemplates={userTemplates} busy={isSelecting} onSelect={select} onClose={()=>setOpen(false)}/>:null}</section>}
