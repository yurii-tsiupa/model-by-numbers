"use client";
import {ChevronLeft,ChevronRight,X} from "lucide-react";
import {useTranslation} from "@/features/i18n/hooks/useTranslation";
import type {ReferenceImage} from "../types/ReferenceImage";
import {ReferenceImageView} from "./ReferenceImageView";
export function ReferenceSplitPanel({reference,references,onSelect,onClose}:{reference:ReferenceImage;references:ReferenceImage[];onSelect:(id:string)=>void;onClose:()=>void}){const {t}=useTranslation();const index=references.findIndex(row=>row.id===reference.id);return <section className="flex min-h-[20rem] flex-1 flex-col border-l border-white/10 bg-neutral-950"><header className="flex items-center justify-between border-b border-white/10 p-3"><div><p className="truncate text-sm text-white">{reference.name}</p><p className="text-xs text-neutral-500">{t(`references.type.${reference.type}`)}{reference.includeInGuide?` · ${t("references.included")}`:""}</p></div><button onClick={onClose} aria-label={t("common.close")}><X className="h-4 w-4"/></button></header><div className="flex min-h-0 flex-1 items-center justify-center p-3"><ReferenceImageView blob={reference.blob} alt={reference.name} className="max-h-full max-w-full object-contain"/></div><footer className="flex justify-center gap-4 p-3"><button disabled={index<=0} onClick={()=>onSelect(references[index-1].id)} aria-label={t("common.previous")}><ChevronLeft/></button><button disabled={index>=references.length-1} onClick={()=>onSelect(references[index+1].id)} aria-label={t("common.next")}><ChevronRight/></button></footer></section>}
