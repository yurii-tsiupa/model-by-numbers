import type {DetectedModelPart} from "./DetectedModelPart";import type {ModelImportWarning} from "./ModelImportWarning";import type {ModelImportError} from "./ModelImportError";
export type ModelPerformanceLevel="light"|"medium"|"heavy"|"very-heavy";
export type ModelAnalysis={file:{name:string;extension:string;mimeType:string;sizeBytes:number};scene:{objectsCount:number;groupsCount:number;meshesCount:number;skinnedMeshesCount:number;emptyObjectsCount:number};geometry:{verticesCount:number;trianglesCount:number;geometriesCount:number;indexedGeometriesCount:number;nonIndexedGeometriesCount:number;invalidGeometriesCount:number};materials:{materialsCount:number;texturedMaterialsCount:number;transparentMaterialsCount:number;missingMaterialsCount:number};textures:{texturesCount:number;estimatedMemoryBytes:number;oversizedTexturesCount:number};bounds:{width:number;height:number;depth:number;center:[number,number,number];isValid:boolean};parts:DetectedModelPart[];performanceLevel:ModelPerformanceLevel;warnings:ModelImportWarning[];errors:ModelImportError[]};
