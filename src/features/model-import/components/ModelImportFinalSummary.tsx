"use client";
import {CheckCircle2} from "lucide-react";import {useTranslation} from "@/features/i18n/hooks/useTranslation";import {formatLocalizedNumber} from "@/features/i18n/lib/i18n";import type {ModelAnalysis} from "../types/ModelAnalysis";import type {ModelDimensions,ModelUnits} from "../types/ModelUnits";import type {GuideCapability} from "../lib/getModelGuideCapabilities";import {formatModelDimension} from "../lib/modelDimensions";import {ModelImportCapabilities} from "./ModelImportCapabilities";
export function ModelImportFinalSummary({name,fileName,printer,material,baseColor,analysis,included,excluded,dimensions,units,orientationAdjusted,warningCount,capabilities}:{name:string;fileName:string;printer:string;material:string;baseColor:string;analysis:ModelAnalysis;included:number;excluded:number;dimensions:ModelDimensions;units:ModelUnits;orientationAdjusted:boolean;warningCount:number;capabilities:readonly GuideCapability[]}){const{t,locale}=useTranslation();const dimensionText=`${formatModelDimension(dimensions.width,locale)} × ${formatModelDimension(dimensions.height,locale)} × ${formatModelDimension(dimensions.depth,locale)} ${t(`modelImport.units.symbol.${units}`)}`;const rows=[[t("modelImport.final.project"),name],[t("guide.printer"),printer],[t("guide.material"),material],[t("modelImport.final.file"),fileName],[t("modelImport.report.format"),t(`modelImport.formats.${analysis.format??"glb"}`)],[t("modelImport.final.parts"),t("modelImport.final.partsValue",{included,excluded})],[t("modelImport.report.triangles"),formatLocalizedNumber(analysis.geometry.trianglesCount,locale)],[t("modelImport.report.dimensions"),dimensionText],[t("modelImport.report.performance"),t(`modelImport.performance.${analysis.performanceLevel}`)],[t("modelImport.final.orientation"),t(orientationAdjusted?"common.yes":"common.no")]];return <section id="import-step-review" className="scroll-mt-40 space-y-4 rounded-2xl border border-emerald-400/15 bg-emerald-400/[.035] p-4 sm:p-5"><div className="flex items-start gap-3"><CheckCircle2 className="mt-0.5 h-5 w-5 text-emerald-400"/><div><h3 className="font-semibold text-white">{t("modelImport.final.title")}</h3><p className="mt-1 text-xs text-neutral-500">{t("modelImport.final.description")}</p></div></div><div className="grid gap-2 sm:grid-cols-2 lg:grid-cols-3">{rows.map(([label,value])=><div key={String(label)} className="rounded-xl border border-white/10 bg-black/15 p-3"><p className="text-[10px] text-neutral-600">{label}</p><p className="mt-1 truncate text-xs text-neutral-200">{value}</p></div>)}<div className="rounded-xl border border-white/10 bg-black/15 p-3"><p className="text-[10px] text-neutral-600">{t("guide.baseColor")}</p><div className="mt-1 flex items-center gap-2 text-xs text-neutral-200"><span className="h-4 w-4 rounded-full border border-white/20" style={{backgroundColor:baseColor}}/>{baseColor.toUpperCase()}</div></div></div><ModelImportCapabilities items={capabilities}/><p className={warningCount?"text-xs text-orange-300":"text-xs text-emerald-300"}>{warningCount?t("modelImport.final.warnings",{count:warningCount}):t("modelImport.final.noWarnings")}</p></section>}
