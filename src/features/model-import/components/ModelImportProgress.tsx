"use client";import {LoaderCircle} from "lucide-react";import {useTranslation} from "@/features/i18n/hooks/useTranslation";
export function ModelImportProgress({progress,stage,onCancel}:{progress:number;stage:string;onCancel:()=>void}){const{t}=useTranslation();const key=stage==="geometry"?"modelImport.status.geometry":stage==="materials"?"modelImport.status.materials":stage==="parts"?"modelImport.status.parts":stage==="report"?"modelImport.status.report":stage==="parsing"?"modelImport.status.parsing":"modelImport.status.reading";return <div role="status" aria-live="polite" className="rounded-2xl border border-orange-400/15 bg-orange-400/5 p-4"><div className="flex items-center gap-2 text-sm"><LoaderCircle className="h-4 w-4 animate-spin text-orange-400"/><span>{t(key)}</span><span className="ml-auto text-orange-300">{Math.round(progress)}%</span></div><div className="mt-3 h-1.5 overflow-hidden rounded-full bg-white/10"><div className="h-full bg-orange-400" style={{width:`${progress}%`}}/></div><button type="button" onClick={onCancel} className="mt-3 text-xs text-neutral-400">{t("modelImport.actions.cancel")}</button></div>}
