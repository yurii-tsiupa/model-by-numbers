"use client";
import {Bounds,OrbitControls} from "@react-three/drei";import {Canvas} from "@react-three/fiber";import {useEffect,useMemo} from "react";import {BoxHelper,Color,type Object3D} from "three";
function PreviewScene({source,selectedUuid,isolate}:{source:Object3D;selectedUuid:string|null;isolate:boolean}){useEffect(()=>{const visibility=new Map<string,boolean>();source.traverse(object=>{visibility.set(object.uuid,object.visible);object.visible=!isolate||object.uuid===selectedUuid||object.children.some(child=>child.uuid===selectedUuid);});return()=>source.traverse(object=>{object.visible=visibility.get(object.uuid)??object.visible;});},[isolate,selectedUuid,source]);const selected=useMemo(()=>{let found:Object3D|null=null;source.traverse(object=>{if(object.uuid===selectedUuid)found=object;});return found;},[selectedUuid,source]);const helper=useMemo(()=>selected?new BoxHelper(selected,new Color("#fb923c")):null,[selected]);useEffect(()=>()=>helper?.dispose(),[helper]);return <Bounds fit clip observe margin={1.25}><primitive object={source}/>{helper?<primitive object={helper}/>:null}</Bounds>}
export function ModelImportPreview({scene,selectedUuid,isolate}:{scene:Object3D;selectedUuid:string|null;isolate:boolean}){return <div className="h-64 overflow-hidden rounded-xl border border-white/10 bg-neutral-900"><Canvas camera={{position:[5,4,6],fov:42}}><ambientLight intensity={1.4}/><directionalLight position={[5,8,5]} intensity={2}/><PreviewScene source={scene} selectedUuid={selectedUuid} isolate={isolate}/><OrbitControls makeDefault/></Canvas></div>}
