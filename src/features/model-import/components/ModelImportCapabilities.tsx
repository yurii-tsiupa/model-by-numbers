"use client";
import {CheckCircle2,XCircle} from "lucide-react";import {useTranslation} from "@/features/i18n/hooks/useTranslation";import type {GuideCapability} from "../lib/getModelGuideCapabilities";
export function ModelImportCapabilities({items}:{items:readonly GuideCapability[]}){const{t}=useTranslation();return <section><h4 className="text-sm font-medium text-white">{t("modelImport.capabilitySummary.title")}</h4><div className="mt-2 grid gap-2 sm:grid-cols-2">{items.map(item=><div key={item.code} className="flex items-center gap-2 rounded-lg border border-white/10 bg-white/[.02] px-3 py-2 text-xs">{item.available?<CheckCircle2 className="h-4 w-4 shrink-0 text-emerald-400"/>:<XCircle className="h-4 w-4 shrink-0 text-neutral-600"/>}<span className={item.available?"text-neutral-300":"text-neutral-600"}>{t(`modelImport.capabilitySummary.${item.code}`)}</span><span className="sr-only">{t(item.available?"modelImport.capabilitySummary.available":"modelImport.capabilitySummary.unavailable")}</span></div>)}</div></section>}
