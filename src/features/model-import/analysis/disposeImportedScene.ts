import type {Material,Object3D,Texture} from "three";
const textureFields=["map","normalMap","roughnessMap","metalnessMap","emissiveMap","alphaMap","aoMap","bumpMap","displacementMap"] as const;
export function disposeImportedScene(scene:Object3D):void{const materials=new Set<Material>(),textures=new Set<Texture>();scene.traverse(object=>{if("geometry" in object){const geometry=(object as Object3D&{geometry?:{dispose():void}}).geometry;geometry?.dispose();}if("material" in object){const value=(object as Object3D&{material?:Material|Material[]}).material;for(const material of Array.isArray(value)?value:value?[value]:[]){materials.add(material);for(const field of textureFields){const texture=(material as Material&Partial<Record<(typeof textureFields)[number],Texture>>)[field];if(texture)textures.add(texture);}}}});textures.forEach(texture=>texture.dispose());materials.forEach(material=>material.dispose());}
