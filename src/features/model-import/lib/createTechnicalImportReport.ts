import type {ModelAnalysis} from "../types/ModelAnalysis";import type {ModelFormat} from "../types/ModelFormat";
export function createTechnicalImportReport({analysis,format,userAgent}:{analysis:ModelAnalysis;format:ModelFormat;userAgent:string}):string{return[`Model by Numbers import report`,`Format: ${format}`,`File size: ${analysis.file.sizeBytes}`,`Meshes: ${analysis.scene.meshesCount}`,`Parts: ${analysis.parts.length}`,`Triangles: ${analysis.geometry.trianglesCount}`,`Materials: ${analysis.materials.materialsCount}`,`Textures: ${analysis.textures.texturesCount}`,`Performance: ${analysis.performanceLevel}`,`Warnings: ${analysis.warnings.map(item=>item.code).join(", ")||"none"}`,`Errors: ${analysis.errors.map(item=>item.code).join(", ")||"none"}`,`Browser: ${userAgent}`].join("\n");}
