"use client";
import { useTranslation } from "@/features/i18n/hooks/useTranslation";
import { formatLocalizedDate } from "@/features/i18n/lib/i18n";
import type { GuideLibraryTemplate } from "../types/GuideLibraryTemplate";
import { TemplatePreview } from "./TemplatePreview";

export function TemplateCard({template,name,busy,onPreview,onDuplicate,onDelete}:{template:GuideLibraryTemplate;name:string;busy:boolean;onPreview:()=>void;onDuplicate:()=>void;onDelete?:()=>void}){const{t,locale}=useTranslation();return <article className="flex min-w-0 flex-col rounded-2xl border border-[var(--border)] bg-[var(--card)] p-4"><TemplatePreview settings={template.settings}/><div className="mt-4 flex items-start justify-between gap-3"><div className="min-w-0"><h3 className="truncate text-base font-semibold">{name}</h3><p className="mt-1 text-xs text-[var(--text-secondary)]">{t(`templates.categories.${template.category}`)} · {t(template.source==="builtIn"?"templates.card.builtIn":"templates.card.user")}</p></div></div>{template.source==="user"?<p className="mt-2 text-xs text-[var(--text-secondary)]">{t("templates.card.updated",{date:formatLocalizedDate(template.updatedAt,locale,{day:"2-digit",month:"short",year:"numeric"})})}</p>:null}<div className="mt-4 flex flex-wrap gap-2"><button type="button" onClick={onPreview} aria-label={t("templates.accessibility.preview",{name})} className="min-h-10 flex-1 rounded-[10px] border border-[var(--border)] px-3 text-sm font-medium focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[var(--accent)]">{t("templates.actions.preview")}</button><button type="button" disabled={busy} onClick={onDuplicate} aria-label={t("templates.accessibility.duplicate",{name})} className="min-h-10 flex-1 rounded-[10px] bg-[var(--accent)] px-3 text-sm font-medium text-[var(--accent-foreground)] disabled:opacity-50 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[var(--accent)]">{busy?t("templates.actions.duplicating"):t("templates.actions.duplicate")}</button>{onDelete?<button type="button" disabled={busy} onClick={onDelete} aria-label={t("templates.accessibility.delete",{name})} className="min-h-10 rounded-[10px] border border-[var(--border)] px-3 text-sm text-[var(--text-secondary)] disabled:opacity-50 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[var(--accent)]">{t("templates.actions.delete")}</button>:null}</div></article>}
