"use client";
import {ArrowDown,ArrowUp,Check,ChevronDown,Copy,GripVertical,MapPin,Paintbrush,Pencil,Plus,RotateCcw,Trash2} from "lucide-react";
import {useRef,useState} from "react";
import {ConfirmationModal} from "@/components/ui/ConfirmationModal";
import {useTranslation} from "@/features/i18n/hooks/useTranslation";
import {getPaintingStageLabel,getPaintingStageTypeLabel} from "../../lib/paintingStageLabel";
import {useModelEditorStore} from "../../store/modelEditorStore";
import {invalidatePaintingStepPreview} from "../../step-previews/stepPreviewService";
import type {PaintingStage} from "../../types/PaintingWorkflow";
import type {ManualDetail} from "@/features/models/types/ManualDetail";
import {SimplePaintingStepEditor} from "./SimplePaintingStepEditor";
import {getLegacyTargetModeCounts,type SimpleTargetMode} from "../../lib/simpleTargetMode";

type Props={projectId:string;activeReferenceId:string|null;onSelectReference:(id:string)=>void;onShowReference:(id:string)=>void;onReferenceDeleted:(id:string)=>void};
export function SimplePaintingSteps(props:Props){
 const{t}=useTranslation(),{projectId}=props,parts=useModelEditorStore(s=>s.parts),details=useModelEditorStore(s=>s.manualDetails),palette=useModelEditorStore(s=>s.palette),simpleTargetMode=useModelEditorStore(s=>s.simpleTargetMode),applySimpleTargetMode=useModelEditorStore(s=>s.applySimpleTargetMode),selectedPartId=useModelEditorStore(s=>s.selectedPartId),selectPart=useModelEditorStore(s=>s.selectPart),add=useModelEditorStore(s=>s.addPaintingStage),update=useModelEditorStore(s=>s.updatePaintingStage),removeSimpleStep=useModelEditorStore(s=>s.deleteSimplePaintingStage),move=useModelEditorStore(s=>s.movePaintingStage),duplicate=useModelEditorStore(s=>s.duplicatePaintingStage),setActive=useModelEditorStore(s=>s.setActivePaintingStage),[editingId,setEditingId]=useState<string|null>(null),[draftStage,setDraftStage]=useState<PaintingStage|null>(null),[draftPartId,setDraftPartId]=useState<string|null>(null),[deleting,setDeleting]=useState<PaintingStage|null>(null),[changingMode,setChangingMode]=useState(false);
 const snapshotRef=useRef<{manualDetails:ManualDetail[];nextManualDetailNumber:number;stage:PaintingStage|null;partId:string}|null>(null);
 const part=parts.find(item=>item.id===draftPartId)??parts.find(item=>editingId!==null&&item.paintingWorkflow.stages.some(stage=>stage.id===editingId))??parts.find(item=>item.id===selectedPartId)??null;
 function captureSnapshot(ownerId:string,stage:PaintingStage|null){const state=useModelEditorStore.getState();snapshotRef.current={manualDetails:structuredClone(state.manualDetails),nextManualDetailNumber:state.nextManualDetailNumber,stage:stage?structuredClone(stage):null,partId:ownerId}}
 function finishClose(){if(part)selectPart(part.id);setEditingId(null);setDraftStage(null);setDraftPartId(null);setActive(null);const state=useModelEditorStore.getState();state.cancelManualDetailPlacement();state.cancelRegionPlacement()}
 function discardSnapshot(){const snapshot=snapshotRef.current;if(!snapshot)return;const state=useModelEditorStore.getState();state.restoreManualDetailsSnapshot(snapshot.manualDetails,snapshot.nextManualDetailNumber);if(snapshot.stage)update(snapshot.partId,snapshot.stage.id,{type:snapshot.stage.type,customName:snapshot.stage.customName,paletteColorId:snapshot.stage.paletteColorId,recommendedCoats:snapshot.stage.recommendedCoats,notes:snapshot.stage.notes,targetReferences:snapshot.stage.targetReferences,overviewPreviewEnabled:snapshot.stage.overviewPreviewEnabled,previewShots:snapshot.stage.previewShots});snapshotRef.current=null}
 function cancelEditor(){discardSnapshot();finishClose()}
 function confirmDiscard(){if(!editingId&&!draftStage)return true;if(!window.confirm(t("editor.workflow.discardConfirm")))return false;cancelEditor();return true}
 function edit(id:string){if(editingId===id)return;if(!confirmDiscard())return;const owner=parts.find(item=>item.paintingWorkflow.stages.some(stage=>stage.id===id)),stage=owner?.paintingWorkflow.stages.find(item=>item.id===id);if(!owner||!stage)return;captureSnapshot(owner.id,stage);const state=useModelEditorStore.getState();state.cancelManualDetailPlacement();state.cancelRegionPlacement();setDraftStage(null);setDraftPartId(owner.id);setEditingId(id);setActive(id)}
 function save(input:Parameters<typeof add>[1]){if(!part)return;if(draftStage)add(part.id,input);else if(editingId)update(part.id,editingId,input);snapshotRef.current=null;finishClose()}
 function create(){if(!part||editingId||draftStage)return;captureSnapshot(part.id,null);const now=new Date().toISOString(),draft:PaintingStage={id:`draft-${crypto.randomUUID()}`,order:part.paintingWorkflow.stages.length+1,type:"base-coat",customName:t("editor.workflow.defaultStepTitle"),paletteColorId:null,recommendedCoats:null,notes:"",targetReferences:[],overviewPreviewEnabled:true,previewShots:[],createdAt:now,updatedAt:now};setEditingId(null);setDraftPartId(part.id);setDraftStage(draft);setActive(null)}
 function confirmDelete(){if(!part||!deleting)return;invalidatePaintingStepPreview(projectId,deleting.id);removeSimpleStep(part.id,deleting.id);if(editingId===deleting.id){snapshotRef.current=null;finishClose()}setDeleting(null)}
 function copy(stage:PaintingStage){if(!part)return;duplicate(part.id,stage.id);const id=useModelEditorStore.getState().activePaintingStageId;if(id)setEditingId(id)}
 const counts=getLegacyTargetModeCounts(details),mixed=counts.markers>0&&counts.region>0;
 function chooseMode(mode:SimpleTargetMode){
   const destructive=details.some(detail=>(detail.targetMode==="region"||Boolean(detail.region?.selections.length)?"region":"markers")!==mode&&(detail.pins.length>0||Boolean(detail.region?.selections.length)));
   if(changingMode){
     if(!window.confirm(t("editor.simpleTarget.workflowResetConfirm")))return;
     for(const stage of parts.flatMap(item=>item.paintingWorkflow.stages))invalidatePaintingStepPreview(projectId,stage.id);
   }else if(destructive&&!window.confirm(t(mode==="markers"?"editor.simpleTarget.confirmKeepMarkers":"editor.simpleTarget.confirmKeepRegions")))return;
   applySimpleTargetMode(mode);setChangingMode(false);snapshotRef.current=null;finishClose();
 }
 return <section className="simple-painting-steps pt-4">
  {!simpleTargetMode||changingMode?<SimpleTargetModeSetup currentMode={simpleTargetMode} mixed={mixed} counts={counts} changing={changingMode} onCancel={()=>setChangingMode(false)} onContinue={chooseMode}/>:<>
   <div className="flex items-center gap-2.5 rounded-xl border border-[var(--border)] bg-[var(--card)] p-2.5">
    <span className="grid size-8 shrink-0 place-items-center rounded-lg bg-[var(--accent-soft)] text-[var(--accent)]">{simpleTargetMode==="markers"?<MapPin className="size-3.5"/>:<Paintbrush className="size-3.5"/>}</span>
    <span className="min-w-0 flex-1"><span className="block text-[10px] font-semibold uppercase tracking-wide text-[var(--text-muted)]">{t("editor.simpleTarget.settingLabel")}</span><span className="mt-0.5 block truncate text-xs font-semibold text-[var(--text)]">{t(simpleTargetMode==="markers"?"editor.simpleTarget.projectMarkers":"editor.simpleTarget.projectRegions")}</span></span>
    <button type="button" onClick={()=>{if(confirmDiscard())setChangingMode(true)}} className="inline-flex min-h-7 cursor-pointer items-center gap-1.5 rounded-lg border border-[var(--border-strong)] bg-transparent px-2.5 text-[10px] font-semibold text-[var(--text-muted)] transition-colors duration-150 hover:border-[var(--accent)] hover:text-[var(--accent)] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[var(--accent)]"><RotateCcw className="size-3"/>{t("common.change")}</button>
   </div>
   {parts.length?<div className="relative mt-3"><select aria-label={t("editor.accessibility.detailSelect")} value={selectedPartId??""} onChange={event=>{if(confirmDiscard())selectPart(event.target.value)}} className="simple-steps-select h-10 w-full cursor-pointer appearance-none rounded-lg bg-[var(--card)] py-0 pl-3 pr-10 text-sm outline-none"><option value="" disabled>{t("editor.colors.selectDetail")}</option>{parts.map(item=><option key={item.id} value={item.id}>{item.name}</option>)}</select><ChevronDown aria-hidden="true" className="pointer-events-none absolute right-3 top-1/2 size-4 -translate-y-1/2 text-[var(--text-muted)]"/></div>:null}
   {part&&(part.paintingWorkflow.stages.length||draftStage)?<ol className="mt-4 space-y-4">{[...part.paintingWorkflow.stages,...(draftStage?[draftStage]:[])].map((stage,index,visibleStages)=>{
    const isDraft=stage.id===draftStage?.id,expanded=isDraft||editingId===stage.id,title=getPaintingStageLabel(stage,t),detailReferences=stage.targetReferences?.filter(reference=>reference.type==="manualDetail")??[],targetDetail=details.find(detail=>detail.id===detailReferences[0]?.id),color=palette.find(item=>item.id===targetDetail?.colorId),targetSummary=stage.type==="primer"?t("editor.workflow.entireModel"):color?.name??targetDetail?.name??null;
    return <li key={stage.id} className="relative grid grid-cols-[32px_minmax(0,1fr)] gap-3">
     {index<visibleStages.length-1?<span aria-hidden="true" className={`simple-step-connector absolute bottom-[-1rem] left-[15px] top-7 w-px ${visibleStages[index+1]?.id===draftStage?.id?"is-transitioning":""}`}/>:null}
     <button type="button" onClick={()=>{if(!isDraft)edit(stage.id)}} aria-label={title} aria-pressed={expanded} className={`simple-step-number relative z-[1] grid size-7 cursor-pointer place-items-center self-start rounded-full text-xs font-semibold ${expanded?"is-editing":"is-complete"}`}>{index+1}</button>
     <article className={`simple-step-card min-w-0 rounded-xl p-3 ${expanded?"is-selected":""}`}>
      <button type="button" onClick={()=>edit(stage.id)} className="flex w-full cursor-pointer items-start gap-3 text-left">
       <span className="min-w-0 flex-1"><span className="block truncate text-sm font-semibold text-[var(--text)]">{title}</span><span className="mt-0.5 block truncate text-[10px] text-[var(--text-muted)]">{getPaintingStageTypeLabel(stage.type,t)}{targetSummary?` · ${targetSummary}`:""}</span></span>
       <span aria-hidden="true" className="size-6 shrink-0 rounded-md border border-[var(--border)]" style={{backgroundColor:color?.hex??"var(--text-muted)"}}/>
      </button>
      {expanded?<SimplePaintingStepEditor key={stage.id} stage={stage} projectId={projectId} activeReferenceId={props.activeReferenceId} onSelectReference={props.onSelectReference} onShowReference={props.onShowReference} onReferenceDeleted={props.onReferenceDeleted} onClose={cancelEditor} onSave={save}/>:<div className="mt-2 flex items-center gap-1">
       <span className="simple-step-action grid size-8 place-items-center" aria-hidden="true"><GripVertical className="size-4"/></span>
       <button type="button" onClick={()=>edit(stage.id)} aria-label={t("painting.actions.edit")} className="simple-step-action grid size-8 cursor-pointer place-items-center"><Pencil className="size-3.5"/></button>
       <button type="button" onClick={()=>copy(stage)} aria-label={t("editor.steps.actions.duplicate")} className="simple-step-action grid size-8 cursor-pointer place-items-center"><Copy className="size-3.5"/></button>
       <button type="button" disabled={index===0} onClick={()=>part&&move(part.id,stage.id,"up")} aria-label={t("editor.steps.actions.moveUp")} className="simple-step-action grid size-8 cursor-pointer place-items-center disabled:cursor-not-allowed disabled:opacity-35"><ArrowUp className="size-3.5"/></button>
       <button type="button" disabled={index===part.paintingWorkflow.stages.length-1} onClick={()=>part&&move(part.id,stage.id,"down")} aria-label={t("editor.steps.actions.moveDown")} className="simple-step-action grid size-8 cursor-pointer place-items-center disabled:cursor-not-allowed disabled:opacity-35"><ArrowDown className="size-3.5"/></button>
       <button type="button" onClick={()=>setDeleting(stage)} aria-label={t("editor.steps.actions.delete")} className="simple-step-action simple-step-delete grid size-8 cursor-pointer place-items-center"><Trash2 className="size-3.5"/></button>
      </div>}
     </article>
    </li>})}</ol>:<div className="mt-3 rounded-xl border border-[var(--border)] bg-[var(--card)] px-4 py-5 text-center"><span className="mx-auto grid size-10 place-items-center rounded-xl bg-[var(--accent-soft)] text-[var(--accent)]"><Paintbrush className="size-5"/></span><h3 className="mt-3 text-sm font-semibold text-[var(--text)]">{t("editor.steps.empty")}</h3><p className="mx-auto mt-1 max-w-[17rem] text-xs leading-5 text-[var(--text-muted)]">{t("editor.steps.emptyHelp")}</p></div>}
   <button type="button" disabled={!part||editingId!==null||draftStage!==null} onClick={create} className="simple-add-step mt-3 flex min-h-11 w-full cursor-pointer items-center justify-center gap-2 rounded-xl px-3 text-sm font-semibold disabled:cursor-not-allowed disabled:opacity-40"><Plus className="size-4"/>{t("editor.workflow.addStep")}</button>
  </>}
  <ConfirmationModal isOpen={Boolean(deleting)} title={t("editor.steps.delete.title",{title:deleting?getPaintingStageLabel(deleting,t):""})} description={t("editor.steps.delete.description")} confirmLabel={t("editor.steps.actions.delete")} variant="danger" onClose={()=>setDeleting(null)} onConfirm={confirmDelete}/>
 </section>
}
function SimpleTargetModeSetup({currentMode,mixed,counts,changing,onCancel,onContinue}:{currentMode:SimpleTargetMode|null;mixed:boolean;counts:{markers:number;region:number};changing:boolean;onCancel:()=>void;onContinue:(mode:SimpleTargetMode)=>void}){
 const{t}=useTranslation(),[selected,setSelected]=useState<SimpleTargetMode|null>(changing?currentMode:null);
 return <div className="mt-4 rounded-xl border border-[var(--border)] bg-[var(--card)] p-4">{mixed?<><h4 className="text-sm font-semibold">{t("editor.simpleTarget.mixedTitle")}</h4><p className="mt-1 text-xs text-[var(--text-secondary)]">{t("editor.simpleTarget.mixedDescription",{markers:counts.markers,regions:counts.region})}</p></>:<><h4 className="text-sm font-semibold">{t(changing?"editor.region.changeMethod":"editor.simpleTarget.chooseTitle")}</h4><p className="mt-1 text-xs text-[var(--text-secondary)]">{t("editor.simpleTarget.chooseHelp")}</p></>}<div className="mt-3 grid gap-2"><ModeCard mode="markers" selected={selected==="markers"} icon={MapPin} title={t("editor.region.markers")} description={t("editor.simpleTarget.markersDescription")} help={t("editor.simpleTarget.markersBest")} onClick={()=>setSelected("markers")}/><ModeCard mode="region" selected={selected==="region"} icon={Paintbrush} title={t("editor.simpleTarget.regions")} description={t("editor.simpleTarget.regionsDescription")} help={t("editor.simpleTarget.regionsBest")} onClick={()=>setSelected("region")}/></div><div className="mt-4 flex justify-end gap-2">{changing?<button type="button" onClick={onCancel} className="min-h-10 rounded-lg border border-[var(--border)] bg-[var(--card)] px-4 text-sm font-medium hover:bg-[var(--bg)] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[var(--accent)]">{t("common.cancel")}</button>:null}<button type="button" disabled={!selected||selected===currentMode} onClick={()=>selected&&onContinue(selected)} className="min-h-10 rounded-lg bg-[var(--accent)] px-4 text-sm font-semibold text-[var(--accent-foreground)] transition hover:brightness-110 disabled:cursor-not-allowed disabled:opacity-40 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[var(--accent)]">{t(changing?"editor.simpleTarget.confirmChange":"common.continue")}</button></div></div>
}
function ModeCard({selected,icon:Icon,title,description,help,onClick}:{mode:SimpleTargetMode;selected:boolean;icon:typeof MapPin;title:string;description:string;help:string;onClick:()=>void}){return <button type="button" aria-pressed={selected} onClick={onClick} className={`relative flex min-h-24 cursor-pointer items-start gap-3 rounded-xl border p-3 text-left transition focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[var(--accent)] ${selected?"border-[var(--accent)] bg-[color-mix(in_srgb,var(--accent)_12%,var(--card))] ring-1 ring-[var(--accent)]":"border-[var(--border)] bg-[var(--card)] hover:border-[var(--accent)]"}`}><span className={`grid size-9 shrink-0 place-items-center rounded-lg ${selected?"bg-[var(--accent)] text-[var(--accent-foreground)]":"bg-[var(--card)] text-[var(--text-secondary)]"}`}><Icon className="size-4"/></span><span><span className="block text-sm font-semibold text-[var(--text)]">{title}</span><span className="mt-0.5 block text-xs text-[var(--text)]">{description}</span><span className="mt-1 block text-[10px] text-[var(--text-secondary)]">{help}</span></span>{selected?<Check className="absolute right-3 top-3 size-4 text-[var(--accent)]"/>:null}</button>}
