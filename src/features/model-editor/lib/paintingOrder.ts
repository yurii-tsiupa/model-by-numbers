export type PaintingOrderPart={id:string;includeInGuide?:boolean};
export function normalizePaintingOrder({paintingOrder,parts}:{paintingOrder:readonly string[]|null|undefined;parts:readonly PaintingOrderPart[]}):string[]{const valid=new Set(parts.map(p=>p.id)),seen=new Set<string>(),result:string[]=[];for(const id of paintingOrder??[]){if(valid.has(id)&&!seen.has(id)){seen.add(id);result.push(id)}}for(const part of parts)if(!seen.has(part.id)){seen.add(part.id);result.push(part.id)}return result;}
export function isPaintingOrderConfigured({paintingOrder,parts}:{paintingOrder:readonly string[];parts:readonly PaintingOrderPart[]}){if(!parts.length)return false;const normalized=normalizePaintingOrder({paintingOrder,parts});return normalized.length===paintingOrder.length&&normalized.every((id,index)=>id===paintingOrder[index]);}
export function getPartsInPaintingOrder<T extends PaintingOrderPart>({parts,paintingOrder,includeExcluded=true}:{parts:readonly T[];paintingOrder:readonly string[];includeExcluded?:boolean}):T[]{const byId=new Map(parts.map(p=>[p.id,p]));return normalizePaintingOrder({paintingOrder,parts}).flatMap(id=>{const part=byId.get(id);return part&&(includeExcluded||part.includeInGuide!==false)?[part]:[]});}
